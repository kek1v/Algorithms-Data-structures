#include<iostream>
#include<vector>
#include"graph_storages.hpp"

just_dfs(const graph_t *graph, vertex_t start, std::vector<bool> used){
    used[start] = true; 
    for(auto neighbour)
}

int main(){
    
    return 0;
}